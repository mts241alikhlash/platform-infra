# GENERATED FILE - DO NOT EDIT BY HAND.
#
# Environment: staging
# Sources:     gateway/schema/routing-manifest.schema.json
#              gateway/manifests/academic.json sha256:f7a985b1100dca89306a36b23bb03b62899a07efe89b0e69ece76cbb13c59daf
#              gateway/manifests/admission.json sha256:f515c09303ad49bcf9cdd36e5798924beaf78e2a3d5e68d6447fc0502e3ccfa6
#              gateway/manifests/admin.json sha256:8e4b02fedaa02592633bbcfd99a225c9153f711eef11a115fccc2cc88fea1eaf
#              gateway/manifests/assessment.json sha256:baccce806519f1a63a9e0a83a759b1b75caf6fdb979f34649c1103f7da66152b
#              gateway/manifests/hr.json sha256:26ef85a16d7cc0bed0321f80cd404f9c7b5ad457525cb83e3bc2682c39fb6a8a
#              gateway/manifests/inventory.json sha256:82b5bf632d9f1dcf389d6a98c602c3474c2451e1ceed58d12a7ecc0a70c2a575
#              gateway/manifests/portal.json sha256:22787a2217121fa5ea5845cf22378adc9ab3631311467b40baadd8b9a16ba5df
#              upstreams/services.json
#              deployment/staging.lock.json
# Generator:   gateway/generate.mjs
# Regenerate:  node gateway/generate.mjs
# Verify:      node gateway/generate.mjs --check
#
# One origin serves each SPA and its API paths. This preserves same-site
# authentication cookies while keeping service-to-service traffic private.
#
# Docker embedded DNS follows recreated service containers after its validity
# window. Every upstream remains named directly in proxy_pass for URI stability.
#
# Nginx refuses a UTF-8 BOM; this file is emitted as UTF-8 without one.
resolver 127.0.0.11 valid=10s ipv6=off;
server_tokens off;

upstream academic_service {
    zone academic_service 64k;
    server academic-service:3200 resolve;
}

upstream admission_service {
    zone admission_service 64k;
    server admission-service:3700 resolve;
}

upstream assessment_service {
    zone assessment_service 64k;
    server assessment-service:4000 resolve;
}

upstream hr_service {
    zone hr_service 64k;
    server hr-service:3800 resolve;
}

upstream identity_service {
    zone identity_service 64k;
    server identity-service:3000 resolve;
}

upstream inventory_service {
    zone inventory_service 64k;
    server inventory-service:3300 resolve;
}

upstream portal_service {
    zone portal_service 64k;
    server portal-service:3600 resolve;
}

upstream presence_service {
    zone presence_service 64k;
    server presence-service:3400 resolve;
}

upstream student_service {
    zone student_service 64k;
    server student-service:3900 resolve;
}

upstream academic_web {
    zone academic_web 64k;
    server academic-web:8080 resolve;
}

upstream admin_web {
    zone admin_web 64k;
    server admin-web:8080 resolve;
}

upstream admission_web {
    zone admission_web 64k;
    server admission-web:8080 resolve;
}

upstream assessment_web {
    zone assessment_web 64k;
    server assessment-web:8080 resolve;
}

upstream hr_web {
    zone hr_web 64k;
    server hr-web:8080 resolve;
}

upstream inventory_web {
    zone inventory_web 64k;
    server inventory-web:8080 resolve;
}

upstream portal_web {
    zone portal_web 64k;
    server portal-web:8080 resolve;
}

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# Only browser navigations may use the SPA shell for an unmatched path.
# Fetch/XHR requests receive JSON 404 instead of successful HTML.
map $http_sec_fetch_mode $serve_spa_shell {
    default       1;
    "cors"        0;
    "same-origin" 0;
    "no-cors"     0;
}

# ===========================================================================
# academic-web
# ===========================================================================
server {
    listen 80 default_server;
    server_name _;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # -------------------------------------------------------- academic
    location ~ ^/(academic-calendar-types|academic-calendars|academic-settings|academic-years|classroom-structures|classroom-supervisors|classrooms|curricula|curriculum-subjects|grade-academic-years|grades|occupations|schedules|semester-rollovers|semester-types|semesters|subjects|teaching-assignments|time-slots)(/|$) {
        proxy_pass         http://academic_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # -------------------------------------------------------------- hr
    location ~ ^/(employees)(/|$) {
        proxy_pass         http://hr_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # -------------------------------------------------------- identity
    location ~ ^/(accounts|audit-logs|auth|blood-types|permissions|profiles|religions|roles|school-unit-addresses|school-unit-social-medias|school-unit-types|school-units|sessions|users)(/|$) {
        proxy_pass         http://identity_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # --------------------------------------------------------- student
    location ~ ^/(parents|student-enrollments|student-graduations|student-parents|student-promotions|students)(/|$) {
        proxy_pass         http://student_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    location = /health/academic {
        proxy_pass         http://academic_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/hr {
        proxy_pass         http://hr_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/identity {
        proxy_pass         http://identity_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/student {
        proxy_pass         http://student_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # -------------------------------------------------------- unrouted
    # No known API prefixes are intentionally unrouted for this app.

    # ------------------------------------------------------------- web
    # The web image owns the static release and SPA shell.
    location /assets/ {
        proxy_pass         http://academic_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        access_log off;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
    }

    # index.html names the current hashed bundles and must not be cached.
    location = /index.html {
        proxy_pass         http://academic_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        add_header Cache-Control "no-store" always;
    }

    # Browser navigations reach the web image; API-style fetches do not get its SPA fallback.
    location / {
        if ($serve_spa_shell = 0) {
            return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
        }
        proxy_pass         http://academic_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }
}

# ===========================================================================
# admission-web
# ===========================================================================
server {
    listen 80;
    server_name ppdb.localhost;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # -------------------------------------------------------- academic
    location ~ ^/(academic-years)(/|$) {
        proxy_pass         http://academic_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # ------------------------------------------------------- admission
    location ~ ^/(admissions)(/|$) {
        proxy_pass         http://admission_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # -------------------------------------------------------- identity
    location ~ ^/(auth|blood-types|profiles|religions|users)(/|$) {
        proxy_pass         http://identity_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    location = /health/academic {
        proxy_pass         http://academic_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/admission {
        proxy_pass         http://admission_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/identity {
        proxy_pass         http://identity_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # -------------------------------------------------------- unrouted
    # Refuse known API prefixes before the SPA fallback can serve HTML.
    location ~ ^/(settings)(/|$) {
        default_type application/json;
        return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
    }

    # ------------------------------------------------------------- web
    # The web image owns the static release and SPA shell.
    location /assets/ {
        proxy_pass         http://admission_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        access_log off;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
    }

    # index.html names the current hashed bundles and must not be cached.
    location = /index.html {
        proxy_pass         http://admission_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        add_header Cache-Control "no-store" always;
    }

    # Browser navigations reach the web image; API-style fetches do not get its SPA fallback.
    location / {
        if ($serve_spa_shell = 0) {
            return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
        }
        proxy_pass         http://admission_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }
}

# ===========================================================================
# admin-web
# ===========================================================================
server {
    listen 80;
    server_name admin.localhost;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # -------------------------------------------------------- identity
    location ~ ^/(audit-logs|auth|blood-types|permissions|profiles|religions|roles|school-unit-addresses|school-unit-social-medias|school-unit-types|school-units|users)(/|$) {
        proxy_pass         http://identity_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # ---------------------------------------------------------- portal
    location ~ ^/(files)(/|$) {
        proxy_pass         http://portal_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    location = /health/identity {
        proxy_pass         http://identity_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/portal {
        proxy_pass         http://portal_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # -------------------------------------------------------- unrouted
    # No known API prefixes are intentionally unrouted for this app.

    # ------------------------------------------------------------- web
    # The web image owns the static release and SPA shell.
    location /assets/ {
        proxy_pass         http://admin_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        access_log off;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
    }

    # index.html names the current hashed bundles and must not be cached.
    location = /index.html {
        proxy_pass         http://admin_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        add_header Cache-Control "no-store" always;
    }

    # Browser navigations reach the web image; API-style fetches do not get its SPA fallback.
    location / {
        if ($serve_spa_shell = 0) {
            return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
        }
        proxy_pass         http://admin_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }
}

# ===========================================================================
# assessment-web
# ===========================================================================
server {
    listen 80;
    server_name assessment.localhost;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # -------------------------------------------------------- academic
    location ~ ^/(academic-years|classrooms|semesters|subjects|teaching-assignments)(/|$) {
        proxy_pass         http://academic_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # ------------------------------------------------------ assessment
    location ~ ^/(assessment-items|assessment-weights|attendances|dashboards|rapors|student-scores)(/|$) {
        proxy_pass         http://assessment_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # -------------------------------------------------------- identity
    location ~ ^/(auth|blood-types|permissions|profiles|religions|roles|school-units|users)(/|$) {
        proxy_pass         http://identity_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # --------------------------------------------------------- student
    location ~ ^/(student-enrollments|students)(/|$) {
        proxy_pass         http://student_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    location = /health/academic {
        proxy_pass         http://academic_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/assessment {
        proxy_pass         http://assessment_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/identity {
        proxy_pass         http://identity_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/student {
        proxy_pass         http://student_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # -------------------------------------------------------- unrouted
    # No known API prefixes are intentionally unrouted for this app.

    # ------------------------------------------------------------- web
    # The web image owns the static release and SPA shell.
    location /assets/ {
        proxy_pass         http://assessment_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        access_log off;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
    }

    # index.html names the current hashed bundles and must not be cached.
    location = /index.html {
        proxy_pass         http://assessment_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        add_header Cache-Control "no-store" always;
    }

    # Browser navigations reach the web image; API-style fetches do not get its SPA fallback.
    location / {
        if ($serve_spa_shell = 0) {
            return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
        }
        proxy_pass         http://assessment_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }
}

# ===========================================================================
# hr-web
# ===========================================================================
server {
    listen 80;
    server_name hr.localhost;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # -------------------------------------------------------- academic
    location ~ ^/(academic-calendars|academic-years)(/|$) {
        proxy_pass         http://academic_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # -------------------------------------------------------------- hr
    location ~ ^/(employee-positions|employees|employment-types|payroll|position-categories|positions)(/|$) {
        proxy_pass         http://hr_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # -------------------------------------------------------- identity
    location ~ ^/(auth|blood-types|profiles|religions|school-unit-types|school-units|users)(/|$) {
        proxy_pass         http://identity_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # -------------------------------------------------------- presence
    location ~ ^/(presence)(/|$) {
        proxy_pass         http://presence_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # --------------------------------------------------------- student
    location ~ ^/(students)(/|$) {
        proxy_pass         http://student_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    location = /health/academic {
        proxy_pass         http://academic_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/hr {
        proxy_pass         http://hr_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/identity {
        proxy_pass         http://identity_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/presence {
        proxy_pass         http://presence_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/student {
        proxy_pass         http://student_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # -------------------------------------------------------- unrouted
    # Refuse known API prefixes before the SPA fallback can serve HTML.
    location ~ ^/(dashboard|settings)(/|$) {
        default_type application/json;
        return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
    }

    # ------------------------------------------------------------- web
    # The web image owns the static release and SPA shell.
    location /assets/ {
        proxy_pass         http://hr_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        access_log off;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
    }

    # index.html names the current hashed bundles and must not be cached.
    location = /index.html {
        proxy_pass         http://hr_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        add_header Cache-Control "no-store" always;
    }

    # Browser navigations reach the web image; API-style fetches do not get its SPA fallback.
    location / {
        if ($serve_spa_shell = 0) {
            return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
        }
        proxy_pass         http://hr_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }
}

# ===========================================================================
# inventory-web
# ===========================================================================
server {
    listen 80;
    server_name simas.localhost;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # -------------------------------------------------------- identity
    location ~ ^/(auth|blood-types|profiles|religions|school-unit-types)(/|$) {
        proxy_pass         http://identity_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # ------------------------------------------------------- inventory
    location ~ ^/(inventory)(/|$) {
        proxy_pass         http://inventory_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    location = /health/identity {
        proxy_pass         http://identity_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/inventory {
        proxy_pass         http://inventory_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # -------------------------------------------------------- unrouted
    # Refuse known API prefixes before the SPA fallback can serve HTML.
    location ~ ^/(dashboard|settings)(/|$) {
        default_type application/json;
        return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
    }

    # ------------------------------------------------------------- web
    # The web image owns the static release and SPA shell.
    location /assets/ {
        proxy_pass         http://inventory_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        access_log off;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
    }

    # index.html names the current hashed bundles and must not be cached.
    location = /index.html {
        proxy_pass         http://inventory_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        add_header Cache-Control "no-store" always;
    }

    # Browser navigations reach the web image; API-style fetches do not get its SPA fallback.
    location / {
        if ($serve_spa_shell = 0) {
            return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
        }
        proxy_pass         http://inventory_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }
}

# ===========================================================================
# portal-web
# ===========================================================================
server {
    listen 80;
    server_name portal.localhost;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # -------------------------------------------------------- identity
    location ~ ^/(auth|blood-types|profiles|religions|school-unit-types|school-units|users)(/|$) {
        proxy_pass         http://identity_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    # ---------------------------------------------------------- portal
    location ~ ^/(files|portal)(/|$) {
        proxy_pass         http://portal_service;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }

    location = /health/identity {
        proxy_pass         http://identity_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /health/portal {
        proxy_pass         http://portal_service/health;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # -------------------------------------------------------- unrouted
    # Refuse known API prefixes before the SPA fallback can serve HTML.
    location ~ ^/(settings)(/|$) {
        default_type application/json;
        return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
    }

    # ------------------------------------------------------------- web
    # The web image owns the static release and SPA shell.
    location /assets/ {
        proxy_pass         http://portal_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        access_log off;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
    }

    # index.html names the current hashed bundles and must not be cached.
    location = /index.html {
        proxy_pass         http://portal_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
        add_header Cache-Control "no-store" always;
    }

    # Browser navigations reach the web image; API-style fetches do not get its SPA fallback.
    location / {
        if ($serve_spa_shell = 0) {
            return 404 '{"statusCode":404,"message":"Route not handled by gateway"}';
        }
        proxy_pass         http://portal_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_pass_header  Set-Cookie;
    }
}
