# Deployment locks

Locks pin web images, service images, gateway image, routing manifest versions,
and manifest checksums. `REPLACE_BEFORE_DEPLOYMENT` is an intentional blocker,
not a deployable image reference.
